# Vex RenderDoc Example

This example shows an integration with RenderDoc in Vex.

Launching this alone will not do much. If you launch it with RenderDoc though, it will capture the first frame. This allows the developer to control when the capture is taken instead of having it be on a frame by frame basis